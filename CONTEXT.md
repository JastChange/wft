# WFT 节点诊断

WFT 帮助单个运维人员在目标 Linux 节点上运行可信的只读检查，用于验证操作系统安装结果，或者为判断故障原因收集完整证据。

## Language

**诊断操作员**：
负责发起节点诊断并查看结果的单个运维人员。
_Avoid_：管理员、平台用户、多租户用户

**节点诊断**：
在一个或多个目标节点上执行只读检查并保留判断依据的工作过程。
_Avoid_：周期巡检、实时监控、部署、远程管理

**安装验证**：
对新安装操作系统的节点执行一次节点诊断，以确认安装结果是否符合预期。
_Avoid_：长期健康巡检、持续监控

**故障诊断**：
对发生问题的节点执行节点诊断，为操作员判断故障原因提供证据。
_Avoid_：自动修复、自动根因裁决

**目标节点**：
诊断操作员明确选择进行安装验证或故障诊断的 Linux 主机。
_Avoid_：自动发现设备、远端 Agent、长期纳管资产

**诊断脚本**：
经过审核、登记并以不可变版本标识的只读检查程序。
_Avoid_：临时 Shell 命令、可修改节点状态的脚本

**诊断任务**：
诊断操作员通过 CLI 明确发起的一次安装验证或故障诊断，包含选定的目标节点和诊断脚本。
_Avoid_：计划任务、后台自动任务、可恢复工作流

**任务状态**：
诊断任务的执行生命周期状态：`PENDING`、`RUNNING`、`COMPLETED`、`FAILED` 或 `CANCELLED`；它不表示目标节点是否存在诊断问题。
_Avoid_：安装结论、诊断问题、错误类型

**诊断方案**：
为一种诊断目的预先定义的有序诊断脚本集合。
_Avoid_：任意 Shell 命令、可变脚本列表

**节点名**：
诊断操作员提供的目标节点名称；诊断任务保存当时的连接信息快照，因此后续重装或修改不会改变历史记录。
_Avoid_：IP 地址身份、永久机器身份

**诊断记录**：
一次诊断任务留下的完整判断依据，包括节点结果、脚本版本、尝试过程和原始输出。
_Avoid_：仅成功/失败状态、临时控制台输出

**安装结论**：
安装验证对单个目标节点给出的 `PASS`、`FAIL` 或 `INCONCLUSIVE` 判断；未完成验证不等同于验证失败。
_Avoid_：任务状态、自动根因

**诊断证据**：
故障诊断收集并保留、供诊断操作员判断原因的事实材料，不自动宣称根因。
_Avoid_：自动根因结论、修复建议

**诊断问题**：
在一个目标节点上识别出的一个具体异常，例如内存问题；一个问题可以引用该节点的多个脚本结果和证据。
_Avoid_：脚本执行、整个诊断任务、跨节点汇总

**待分类问题**：
尚未经过 AI 分析归并的失败检查；AI 可以将同一目标节点的多个待分类问题归并为一个诊断问题，但必须保留来源关系。
_Avoid_：已确认根因、被删除的失败记录

**控制服务器**：
唯一负责执行节点诊断、保存诊断记录并向诊断操作员提供结果页的主机。
_Avoid_：中央平台、执行器集群、远端 Agent

**脚本源**：
由诊断操作员指定并直接信任、供控制服务器同步诊断脚本的 Git 仓库和分支。
_Avoid_：未配置的仓库、临时脚本目录

**脚本快照**：
诊断任务开始时取得的脚本提交及其内容；通常使用脚本源的最新提交，拉取失败时可由诊断操作员明确接受已有缓存。同一任务的所有目标节点使用同一快照。
_Avoid_：仅分支名、任务执行中变化的脚本版本

**结果页**：
供诊断操作员远程查看诊断记录的只读 Web 界面。
_Avoid_：任务控制台、脚本编辑器、运维管理平台

**诊断通知**：
在安装验证异常、故障诊断完成或任务中止时，通过 Webhook 告知诊断操作员任务摘要和结果页位置。
_Avoid_：原始输出副本、诊断结论、任务状态本身

**AI 分析**：
诊断操作员对已有诊断记录手动发起的结构化辅助分析，包含可能原因、证据引用、置信度和下一步检查建议。
_Avoid_：事实证据、自动根因裁决、自动修复

**规则摘要**：
AI 服务不可用或输出无效时，根据诊断记录生成的确定性摘要。
_Avoid_：AI 分析、静默失败

**永久保留**：
系统不会按时间自动删除诊断记录，但诊断操作员可以主动清理且删除后不留记录。
_Avoid_：不可删除、保证永不丢失、固定期限保留

**节点清单**：
诊断操作员在控制服务器上维护的目标节点集合及其连接信息。
_Avoid_：自动发现结果、CMDB 镜像
